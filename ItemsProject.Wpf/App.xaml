<views:MvxApplication
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"    
    x:Class="ItemsProject.Wpf.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ItemsProject.Wpf"
    StartupUri="MainWindow.xaml">
    <views:MvxApplication.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="2 0"></Setter>
            <Setter Property="BorderThickness" Value="0"></Setter>
            <Setter Property="Background" Value="#0B5351"></Setter>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="3"></Setter>
                </Style>
            </Style.Resources>
        </Style>
        <Style TargetType="TextBox">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="3"/>
                </Style>
            </Style.Resources>
        </Style>
        

        <Style TargetType="{x:Type ListViewItem}">
            <Style.Setters>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                            <Grid Background="{TemplateBinding Background}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                <TextBlock Foreground="{TemplateBinding Foreground}" Margin="15 0 0 0" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center">
                                    <ContentPresenter />
                                </TextBlock>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                
                <Setter Property="Margin" Value="0 5 0 0"/>
            </Style.Setters>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22393d"></Setter>
                </Trigger>

                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3a4f52" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="lblDesign" TargetType="Label">
            <Setter Property="FontFamily" Value="SourceCodePro-Regular.ttf #Source Code Pro "/>
        </Style>


        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#ced3d4" />
            <!-- Set font color -->
        </Style>

        <!-- Optional: Apply to all Controls (Button, Label, etc.) -->
        <Style TargetType="Control" BasedOn="{StaticResource {x:Type Control}}">
            <Setter Property="Foreground" Value="#ced3d4" />
        </Style>


    </views:MvxApplication.Resources>
   
    
    
</views:MvxApplication>
