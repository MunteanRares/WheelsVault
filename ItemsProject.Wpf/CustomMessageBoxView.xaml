<views:MvxWindow 
        xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
        x:Class="ItemsProject.Wpf.CustomMessageBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="200" Width="400"
        FontFamily="pack://application:,,,/ItemsProject.Wpf;component/Fonts/#Fredoka Medium"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon + Message -->
        <StackPanel Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Center" Orientation="Horizontal">
            <Image Source="{Binding IconSource}" Width="32" Height="32" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="14" Foreground="#09203F"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="5" Command="{Binding ConfirmCommand}">
                <TextBlock Text="Yes" Foreground="#09203F"/>
            </Button>
            <Button Width="80" Margin="5" Command="{Binding CancelCommand}">
                <TextBlock Text="No" Foreground="#09203F"/>
            </Button>
        </StackPanel>
    </Grid>
</views:MvxWindow>