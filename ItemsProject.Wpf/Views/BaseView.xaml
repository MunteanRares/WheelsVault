<views:MvxWpfView
        xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
        xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
        x:Class="ItemsProject.Wpf.Views.BaseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:ItemsProject.Wpf.Views"
        mc:Ignorable="d" 
        FontSize="20"
        FontFamily="pack://application:,,,/ItemsProject.Wpf;component/Fonts/#Source Code Pro"
        Background="#092327"
        d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="30"
                   Text="HotWheels Collection Manager"
                   Grid.ColumnSpan="2"></TextBlock>

        <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" BorderThickness="0 0 0 2" BorderBrush="#00A9A5"></Border>
        

        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" BorderThickness="0 0 2 0" BorderBrush="#00A9A5"/>

            <Button mvx:Bi.nd="Command openAddFolderWindowCommand" Content="+Add Folder" MaxHeight="30" MaxWidth="160"></Button>

            <ListView BorderThickness="0" BorderBrush="#00A9A5"
                      ItemsSource="{Binding Folders}"
                      SelectedItem="{Binding SelectedFolder}"
                      Background="#092327" Grid.Row="1"
                      Grid.Column="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"></TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Border Height="30">
                    <TextBlock Margin="20 5 0 0" Text="Search..." VerticalAlignment="Center"/>
                </Border>
                <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Grid.Column="0" Width="200" Height="30" HorizontalAlignment="Left" Margin="10 0 0 0"/>
            </StackPanel>
            
            <Button Grid.Column="1" Background="#0B5351" Height="30" IsEnabled="{Binding CanPressAddItem}" Width="160" HorizontalAlignment="Right" Margin="0 0 15 0" mvx:Bi.nd="Command openAddItemWindowCommand">+Add Item</Button>

            <ListView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding FolderItems}" SelectedItem="{Binding SelectedItem}" BorderThickness="0" Background="#092327">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
                                    <TextBlock HorizontalAlignment="Left" Margin="0 0 10 0"  Text="{Binding ModelName}"/>
                                    <TextBlock HorizontalAlignment="Left" Text="{Binding ModelReleaseDate}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
                                    <TextBlock Margin="0 0 10 0" Text="Series:"/>
                                    <TextBlock Text="{Binding CollectionName}"/>
                                </StackPanel>
                            </Grid>
                            <Border Width="200" Margin="0 5 0 5" BorderBrush="#3a4f52" BorderThickness="1"></Border>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

    </Grid>
</views:MvxWpfView>
